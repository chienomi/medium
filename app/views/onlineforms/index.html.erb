<div class="ue_45"></div>
<div class="ue_45"></div>
<div style="width:700px;margin:0 auto;">
  <h1 align="center">All Public Reports</h1>
  <div class="row">
    <% @onlineforms.each do |onlineform| %>
    <div class="col-md-12">
      <% if onlineform.disclose == 1 && @user.id != 1 %>
      <h2>Kept in private</h2>
      <br>
      <% elsif onlineform.disclose == 0 || @user.id == 1 %>

        <% if onlineform.whatkind == 0 %>
          <h2>Bug</h2>
        <% elsif onlineform.whatkind == 1 %>
          <h2>Advice</h2>
        <% elsif onlineform.whatkind == 2 %>
          <h2>Report</h2>
        <% elsif onlineform.whatkind == 3 %>
          <h2>Other</h2>
        <% end %>
          <%= onlineform.body %><br><br>
           <% if @user.id == 1 %>
           id: <%= onlineform.user_id %> |
            <%= link_to 'Edit', edit_onlineform_path(onlineform) %> | <%= link_to 'Destroy', onlineform, method: :delete, data: { confirm: 'Are you sure?' } %>
           <% end %>

      <% end %>
    </div>
    <% end %>
  </div>
  <div align="center">
    <%= button_to 'Report', new_onlineform_path, method: :get, class: "btn btn-primary", style: "width:20%"%>
  </div>
</div>
